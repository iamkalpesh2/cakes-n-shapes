/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'instagram.com'], // Allows you to test external images
  },
};

export default nextConfig;